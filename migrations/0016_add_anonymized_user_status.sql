-- 为用户状态添加 'anonymized' 状态支持
-- 这是为了支持GDPR合规和更好的用户账户管理

-- SQLite 不支持直接修改枚举，所以我们需要重建表
-- 但由于这是一个新增值，我们可以直接插入数据时使用新状态

-- 添加注释说明新状态的用途
-- active: 活跃用户
-- disabled: 管理员禁用
-- suspended: 临时暂停
-- deleted: 软删除（保留数据但隐藏用户）
-- anonymized: 数据脱敏（个人信息已清除但保留业务记录）

-- 由于SQLite的限制，此迁移主要用于文档目的
-- 实际的状态更新将在应用层进行验证